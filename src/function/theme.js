import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      // 主色 粉色
      main: "#fe8b83",
    },
    secondary: {
      // 副色 冰箱藍色
      main: "#C7E3EE",
    },
    white: {
      main: "#ffffff",
    },
    black: {
      main: "#00000",
    },
    text: {
      normal: "#444545",
    },
  },
});

export default theme;
